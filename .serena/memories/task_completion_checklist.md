# Task Completion Checklist

After completing any coding task:
1. `flutter analyze` — ensure no lint errors
2. `dart format .` — format to 80-char lines
3. `flutter test` — run all tests (or targeted test file if applicable)
4. If localization strings were added/changed: `flutter gen-l10n`
